<div class="container">
	<div class="row">
		<div class="col-sm-6 col-sm-offset-3">
			<h1><%= form_title %></h1>

				<%= render 'shared/error_messages', object: @post  %>

			<%= form_for @post do |f| %>
				<div class="form-group">
					<%= f.text_field :title, class: "form-control", placeholder: "Titulo del post"  %> 
				</div>				
				<div class="form-group">					
					<%= f.text_area :body, class: "form-control", placeholer: "Descripción" %>
				</div>
				<div class="form-group">
					<%= f.submit form_title, class: "btn btn-primary btn-lg btn-block" %>
				</div>
			<% end %>
		</div>
	</div>
</div>